# Copy this file to .env and fill in real values - .env is gitignored and never committed.
# Spring Boot doesn't load .env files automatically; export these in your shell (or your
# IDE's run configuration) before starting the app, e.g.:
#   export $(grep -v '^#' .env | xargs) && ./mvnw spring-boot:run

DB_PASSWORD=changeme
