export const MY_LOCATION = { lat: 32.0853, lng: 34.7818 }

export const SEARCH_RADIUS_KM = 15

// The signed-in user we act as (this demo has no auth). Must be a real user id the
// backend knows about - DataSeeder creates users with auto-generated ids starting at 1,
// so user 1 always exists. Sent as-is to the API, which expects a numeric Long.
export const CURRENT_USER_ID = 1

export const SPORTS = ['BASKETBALL', 'SOCCER', 'TENNIS', 'VOLLEYBALL']
